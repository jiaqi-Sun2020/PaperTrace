---
name: reader-skill
description: Prepare source-grounded paper Markdown bundles generated by nature-reader for standalone interactive Chinese-English HTML readers, including reader-specific parsing, faithful-translation validation, source anchors, learner-profile annotation metadata, and reader_feedback.json semantics. Use with lean-html-skill for shared HTML shell, feedback UI, and export behavior when an agent is given a paper_reader directory, paper.md with source_map.json/assets, or the user asks to make a browser-friendly bilingual academic reader.
---

# Reader Skill

## Purpose

Prepare a `nature-reader` bundle for a polished standalone HTML reader. This skill owns reader-specific parsing, validation, source-map semantics, and learner-profile annotation metadata. Shared HTML shell behavior, reusable feedback UI, copy/download controls, and future UI code should live in `lean-html-skill` to avoid duplicate HTML/JS across PaperTrace skills.

This is the final delivery stage of the PDF-to-HTML pipeline. `paper.md`, `source_map.json`, `translation_notes.md`, and `reader_wiki/` are internal inputs/evidence, not user-facing completion results. Do not report pipeline success until the audited `reader_interactive.html` exists.

This stage belongs only to **Primary Pipeline 1: Paper Reader HTML**. Do not confuse it with Pipeline 2 daily-briefing publication, Pipeline 3 reviewed chat import, or Pipeline 4 adaptive teaching decisions and evidence handoff.

It does not translate the paper and does not mutate `.agents`.

## v3 Formal-State Override

This section supersedes older references in this document to a monolithic
`completion_ledger.json` or `paper.md` as formal source of truth.

- Use one explicit `--pdf-dir` as the batch input authority. Its immediate
  PDF children are deterministically sorted and hash-snapshotted before any
  reader work begins; nested `*_reader` folders are never inferred as input.
- The canonical state is `reader_wiki/completion_blocks/<stable-id>.json`, one
  atomic, schema-validated record per source block, formula, figure, table,
  algorithm, and reference segment. `completion_run_state.json` is a derived
  resumable summary; it must preserve same-source `pass` records on rerun.
- Compile `reader_wiki/canonical_reader.md` and the compatibility `paper.md`
  only from all-v3-pass records. Never compile formal HTML from legacy
  `paper.md`, a preview, or an old completion ledger.
- `reader_progress.html` is permitted only as an explicitly labelled
  `INCOMPLETE / NOT FORMAL` artifact. It must contain no feedback export,
  profile handoff, or formal manifest contract and can never be renamed to or
  overwrite `reader_interactive.html`.
- Old formal HTML must be marked `reader_wiki/formal_status.json: stale` when
  its source hash, v3 state, or audit is absent. A stale marker invalidates the
  prior HTML; only a later v3 compile/render/adversarial-audit sequence can
  supersede it.

For an end-to-end natural-language trigger, treat the request as explicit
persistent execution: when Codex goal tools are available, create or resume an
unbudgeted active goal before implementation. The first implementation command
must be the formal batch entry point below with `--agent-continuation`. Do not
edit a bundle first. Keep the goal active whenever the emitted contract says
`persistent_goal_action: keep_active`; only `complete` permits completing the
goal and returning the audited HTML. Text such as “not yet complete” belongs
only in commentary and must be followed by more tool work.

Run the only formal batch entry point from `D:\AI\PaperTrace`:

```powershell
python .\skills\reader-skill\scripts\build_formal_reader_batch.py --pdf-dir "C:\Users\SSS\Desktop\PAPER\2026\7" --reader-root "D:\AI\PaperTrace\2026\7" --resume --agent-continuation
```

It emits the exact discovered paths, order, hashes, and embedded
`agent_continuation_contract` as JSON on standard output; it creates no
`.reader_pipeline_runs`, `reader_batch_state.json`, or other batch-history/state
artifact. It activates at most one incomplete paper. Expected incomplete work
is `status: action_required` with a successful default exit, because it is a
current-session authorship checkpoint rather than a terminal command error.
`--strict-exit` restores exit code 1 for CI. The agent must continue while
`final_response_allowed` is false and may report the complete request only when
every selected reader reaches formal pass.

## LLM-Wiki Compile Layer

Formal HTML generation must pass through a persistent normalized layer before writing `reader_interactive.html`.

For every reader bundle, `markdown_reader_to_html.py` first compiles:

```text
<reader-dir>/reader_wiki/
  reader_manifest.json
  concept_ledger.json
  formula_ledger.json
  figure_table_ledger.json
  algorithm_ledger.json
  object_inventory.json
  preflight_manifest.json
  claim_contribution_ledger.json
  annotation_metadata.json
  paper_summary.json
  structure_validation_report.json
  normalized_reader.md
```

Raw sources remain immutable: PDF files, extraction text, source maps, and original reader bundle files are evidence, not direct HTML input. The HTML renderer may only use the normalized reader layer after validation passes.

Each bilingual block in `reader_manifest.json` must keep stable `block_id`, `source_anchor`, `source_page`, `section`, `block_type`, `original`, `zh`, `notes`, `formulas`, `concepts`, `figures`, and `tables` fields.

Each block must also pass bilingual presentation alignment: Original and
Chinese have the same ordered LaTeX component signatures and the same
inline/display classification. Reject one-sided formula chips or display
cards. Reject Markdown headings inside either bilingual field; section titles
belong outside the field and must never terminate a language panel early.

Before accepting that layer, verify the completion ledger certifies 100% source coverage and 100% faithful `Original` blocks, and that its hash-bound preflight manifest passes. Reject missing or merged source IDs, low-fidelity English rewrites, empty evidence hashes, unregistered figure/table/algorithm cards, missing object-inventory rows, stale absolute artifact paths, and a `paper.md` hash that differs from the completion ledger. Concept counts and HTML structure can never compensate for failed source fidelity. Formal Original blocks must repair PDF layout and reconstruct Original-side LaTeX while immutable raw evidence remains separately inspectable; no post-completion normalizer may rewrite them.

Also require product-neutral content provenance: the completion ledger must certify that the current-session primary model directly authored `chinese_translation`, `block_specific_notes`, and `latex_reconstruction`, with `external_translation_backend: none`, and must bind that declaration to `translation_notes.md` by hash. Do not generate formal HTML when this provenance is missing or stale.

For full papers, require a completion-authored `reader_wiki/concept_candidates.json`. The formal Concept Ledger must be paper-specific, source-anchored, at least 90% Chinese-aliased and explained, and free of equation numbers, section labels, layout tokens, or generic uppercase matches. Concept highlighting is source-driven and language-scoped: first identify concepts in `Original` from the English canonical name and controlled English aliases, then allow `中文` to match controlled Chinese aliases only for those same concept IDs. A Chinese word that merely resembles a ledger concept must not create a new block-level mark. Within a bilingual block, the set of highlighted `concept_id` values must be identical on both sides; English-only marks in the Chinese panel are invalid. Longest controlled alias wins when aliases overlap.

The Personal Knowledge Boundary must be visible from both the header and TOC, summarize profile statuses, and never expose an absolute local profile path. Its visible interface language is English except for the values under `Chinese Name` and `Role in This Paper`, which remain Chinese. Use the exact English columns `Concept`, `Personal Status`, `Chinese Name`, `Type`, and `Role in This Paper`; render statuses and concept types as readable English labels, never internal enum tokens.

Full-paper readers also require completion-authored `reader_wiki/paper_summary.json`. Reader-skill validates and renders it but never invents it. The summary must include a detailed Chinese overview plus distinct source-grounded sections for what the paper does, how it works, why it matters, and evidence/scope/limitations. Every item links back to valid completion anchors.

If `structure_validation_report.json` has `status: "fail"`, stop. Do not write a best-effort HTML file.

## End-To-End Output Contract

`reader_interactive.html` means the paper reader is complete enough for real reading:

- every substantive `**Original:**` block has a faithful `**中文:**` translation;
- a detailed, source-anchored paper summary is visible from the header/TOC and links every claim back to formal source blocks;
- every concept highlighted in `Original` has the same `concept_id` highlighted through a controlled Chinese alias in the paired `中文` panel, and vice versa;
- `**注释:**` contains useful paper logic, knowledge-point, formula, figure/table, or reading guidance when needed;
- every figure/table in `source_map.json` appears as an inspectable figure/table card or semantic table near the relevant prose;
- every algorithm in the source appears as a full algorithm card with original numbered steps and Chinese numbered steps; summaries are invalid;
- every bibliography `R###` source block appears in a single original-language reference panel and never in a Chinese translation panel;
- important formulas are reconstructed as LaTeX display math rather than raw PDF extraction noise;
- display-math delimiters contain only valid mathematical content; Chinese prose punctuation such as `。` must remain outside the delimiters;
- `**注释:**` is block-specific and contains no generic scaffolding such as `逻辑位置：本文主题是...` or `标注建议：如果这里有不懂...`;
- no `待忠实翻译`, `中文译意`, `非逐句精翻`, `reading scaffold`, or placeholder Chinese remains;
- strict translation and structure validation pass;
- every normalized bilingual block has a parsed source page and a one-to-one immutable source row;
- feedback controls and learner-profile annotations are available;
- every knowledge mark includes `data-concept`, `data-status`, `data-source-anchor`, `data-concept-type`, `data-alias-zh`, and `title`;
- the feedback panel closes after `Save mark`, Esc, the close button, or blank-page click, while download/copy export remains available.
- Source Page Index links remain plain relative paths such as `assets/source_pages/page-01.png`; inline math and concept highlighting must not run inside `href`, image `src`, file paths, or source-page labels.
- full PDF readers expose every hash-bound `source_map.pages` image in a left-side source-page viewer that synchronizes to `data-source-page` reader blocks, uses only safe relative `assets/source_pages/` paths, and never substitutes a full page for an inline figure card;
- global controls independently collapse/restore bilingual Original panels and the source-page viewer, preserve keyboard/ARIA state and namespaced localStorage state, keep Contents available, and force Original visible in print;
- `Copy feedback for Codex` always fills a visible fallback textarea when clipboard access is unavailable or blocked, so the user can still retrieve feedback JSON.

Do not generate or report any HTML from incomplete translation. The PDF reader pipeline is one-step-to-final: finish faithful translation, figure/table cards, LaTeX formulas, and block-specific notes first, then generate `reader_interactive.html`.

Conversely, do not report a fully validated reader bundle as completion. Continue from the passing bundle through HTML generation and the publishing adversarial audit. Return the HTML path, not the bundle path, as the primary result.

If the user asked for a completed translated reader and the input bundle still contains placeholders, do not stop at draft HTML and do not report missing external translation tooling as a blocker. Route back to `nature-reader` so the current-session primary model directly authors every substantive `中文` block, block-specific note, and LaTeX reconstruction, then rerun this skill in strict mode.

When the bundle was produced by a draft extraction helper, run the official completion pass before invoking this skill:

```powershell
python D:\AI\PaperTrace\skills\nature-reader\scripts\complete_reader_bundle.py <reader-dir>
```

This completion pass validates the internal reader bundle, records its immutable completion ledger, and writes a hash-bound preflight manifest; it is not a pipeline terminal state, a bypass flag, or a user-facing deliverable. It does **not** translate, crop figures, synthesize table cards, repair Markdown, or generate HTML. After it passes, continue immediately through HTML generation and adversarial audit.

Use this split:

- `nature-reader`: PDF/HTML/text -> source-grounded `paper.md`, `source_map.json`, `translation_notes.md`, `assets/`.
- `reader-skill`: completed internal bundle -> final audited `reader_interactive.html` plus its formal manifest.
- `reader-learner`: exported feedback -> `.agents/reader-learner/knowledge_profile.json`.
- `lean-html-skill`: shared standalone HTML shell, reusable feedback panels, copy/download export controls, and future reader HTML UI output.

## Lean HTML Boundary

Prefer moving any reusable HTML/CSS/JS from this skill into `skills/utils/lean-html-skill`.

Keep in `reader-skill`:

- nature-reader Markdown parsing;
- strict translation-contract validation;
- source block, figure/table, MathJax-safe body semantics;
- learner-profile concept matching and reader-specific annotation metadata;
- compatibility wrappers needed by existing `markdown_reader_to_html.py`.

Move or add in `lean-html-skill`:

- generic standalone page shell and shared layout primitives;
- reusable feedback dock/panel UI;
- copy/download JSON export logic;
- localStorage/browser-memory messaging;
- shared status/question/explanation-style form controls.

Do not add new reader-only feedback JavaScript here when the behavior can be expressed as a reusable lean-html component.

## Required Input

Prefer a directory shaped like:

```text
paper_reader/
  paper.md
  source_map.json
  translation_notes.md
  assets/
```

Also accept a direct path to `paper.md`. Resolve image links relative to the Markdown file.

Open `references/nature-reader-markdown-contract.md` when block parsing fails, the Markdown shape is unfamiliar, or the converter is being changed.

## Translation Contract

`**中文:**` must be a faithful translation of the matching `**Original:**` block.

The final HTML converter must reject draft/paraphrase Chinese columns containing markers such as:

- `中文译意`
- `非逐句精翻`
- `待忠实翻译`
- `reading scaffold`
- `translation aid`

There is no draft HTML route in the formal pipeline. If a bundle still has placeholders, summaries, missing figure/table cards, or noisy formulas, stop and fix `paper.md` / `source_map.json` before running the HTML converter.

Missing local model backends, translation packages, or API SDKs are not valid reasons to avoid the translation pass. The current-session primary model must directly author translations, block-specific notes, and LaTeX reconstructions; the rule is independent of the surrounding agent product.

Put these in `**注释:**`, not `**中文:**`:

- paper logic;
- knowledge-point summaries;
- physical or mathematical role;
- figure notes;
- formula verification notes;
- annotation guidance.

## Quick Start

Run from any directory:

```powershell
python D:\AI\PaperTrace\skills\reader-skill\scripts\markdown_reader_to_html.py <reader-dir>
```

Compile only the normalized reader wiki layer:

```powershell
python D:\AI\PaperTrace\skills\reader-skill\scripts\reader_wiki_compile.py <reader-dir> --profile D:\AI\PaperTrace\.agents\reader-learner\knowledge_profile.json
```

Explicit output:

```powershell
python D:\AI\PaperTrace\skills\reader-skill\scripts\markdown_reader_to_html.py <reader-dir> --output <reader-dir>\reader_interactive.html
```

With learner profile:

```powershell
python D:\AI\PaperTrace\skills\reader-skill\scripts\markdown_reader_to_html.py <reader-dir> --output <reader-dir>\reader_interactive.html --profile D:\AI\PaperTrace\.agents\reader-learner\knowledge_profile.json
```

Useful options:

```powershell
--no-embed-assets
--no-feedback-ui
--no-knowledge-annotations
--math-renderer none
--mathjax-url <local-or-remote-mathjax-script>
```

Formula rendering uses MathJax by default for `$...$`, `\(...\)`, `\[...\]`, and `$$...$$`.

After generating a formal reader, run the adversarial audit:

```powershell
python D:\AI\PaperTrace\skills\reader-skill\tests\adversarial_html_audit.py <reader-dir>
```

For a read-only audit that must not update `formal_artifact_manifest.json`, add `--no-write`. A normal publishing audit writes the manifest only after all checks pass; a manifest-write failure is not a successful audit.

Do not report `reader_interactive.html` as successful until this audit passes.

Do not report any intermediate bundle artifact as successful either. The publishing audit is the terminal pipeline gate.

## HTML Requirements

The output must:

- be a complete `<!doctype html>` document with embedded CSS;
- use semantic structure: `header`, `main`, `article`, `section`, `figure`, `table`, `nav`;
- preserve stable source anchors and source labels;
- show substantive source blocks as aligned `Original` and `中文` columns;
- render the model-authored paper summary before the concept ledger and article body;
- place the synchronized original-page viewer in the left reader sidebar above Contents;
- include accessible `Hide/Show Original` and `Hide/Show Source Pages` controls without deleting source DOM content;
- show `注释` as a separate horizontal third column on wide screens;
- keep figures/tables near their Markdown positions;
- render formulas without letting concept highlighting break MathJax;
- create formula components only from explicit, compiler-audited LaTeX delimiters; never auto-promote plain underscore/superscript text on one language side;
- protect Markdown links and local asset paths before applying inline-math wrapping or knowledge annotation;
- keep Source Page Index links clickable and free of generated HTML inside `href`;
- embed local images by default for portability;
- include mobile and print CSS;
- include feedback UI unless explicitly disabled; new or refactored feedback UI should be provided through `lean-html-skill`.

## Figure And Formula Rules

- Never use a full PDF source-page image as the primary inline figure.
- Use extracted images or reliable figure crops in the article flow.
- If no crop exists, the bundle is still draft for that figure/table; do not report it as a completed `reader_interactive.html`.
- For noisy PDF formulas, prefer reconstructed LaTeX display math and preserve extracted text as evidence.
- If a formula cannot be confidently reconstructed, label that fact and link to the source page.
- Generic note scaffolds are invalid in final HTML. Notes must explain the role of the specific claim, equation, figure, table, method step, or result.

## Feedback Workflow

In the generated HTML:

1. Click a highlighted concept, or select arbitrary text and use the free annotation control.
2. Mark `mastered`, `known`, `learning`, `unknown`, or `unrated`.
3. Add note, exact question, question type, explanation style, and selected/source context when useful.
4. Click `Save mark`.
5. Confirm the saved item appears in the feedback panel and, when a source block is detected, as a page badge.
6. Use `Download feedback JSON` or `Copy feedback for Codex`.
7. Import the exported payload with `reader-learner`.

The HTML page stores marks in browser memory/local page state. It does not update `.agents` automatically. This persistence and export behavior should be shared with `lean-html-skill` instead of reimplemented in domain-specific scripts.

`Save mark` closes the annotate panel. `Download feedback JSON` and `Copy feedback for Codex` save the current form state without closing the panel. Copy must populate the fallback export textarea even when clipboard access succeeds.

## Quality Checklist

Before finishing:

1. `reader_interactive.html` exists and is non-empty only for fully translated readers.
2. `Original` count equals `中文` count.
3. Strict translation and structure validation pass for `reader_interactive.html`.
4. `source_map.json` parses if present.
5. Every Markdown image either renders or is reported as missing.
6. Every source-map figure/table has a figure/table card or semantic table in the Markdown.
7. No full source-page image is embedded as a figure substitute.
8. Stable IDs from Markdown appear in HTML.
9. MathJax-compatible formulas remain readable and raw PDF formula noise is not used as final math.
10. Generic template notes such as `逻辑位置：本文主题是...` or `标注建议：如果这里有不懂...` are absent.
11. Feedback UI contains download/copy controls, exact-question fields, free annotation, saved annotation list, and delete controls.
12. Learner-profile highlighting never appears inside HTML attributes, headings, file paths, code spans, or glossary metadata.
13. The final response gives the HTML path, learner profile path when used, and any warnings.
14. `reader_wiki/structure_validation_report.json` reports `pass`.
15. `reader_wiki/concept_ledger.json` contains 30-60 paper concepts for full papers unless the bundle is explicitly a fixture/demo.
16. `reader_wiki/formula_ledger.json` contains reconstructed LaTeX for formula blocks, and MathJax is present in the final HTML.
17. `reader_wiki/algorithm_ledger.json` reports every source algorithm as a full card, not a summary.
18. Source Page Index links contain no `<span class="math-inline">` or other generated markup and their target files exist under `assets/source_pages/`.
19. `Copy feedback for Codex` exposes a fallback textarea populated with the same JSON as the clipboard payload.
20. `python D:\AI\PaperTrace\skills\reader-skill\tests\adversarial_html_audit.py <reader-dir>` passes before success is reported.
21. Every bilingual block has identical ordered Original/Chinese formula-component signatures, and no field-local heading can escape its language panel.
