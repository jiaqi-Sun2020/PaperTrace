interface:
  display_name: "Allegory Teach"
  short_description: "Teach hard concepts with low-load precise bridges."
  default_prompt: "Use $allegory-teach to explain one difficult concept with the simplest concrete bridge that works, adding an analogy only if it reduces cognitive load and using a concealed-name fable only when I explicitly request one."

policy:
  allow_implicit_invocation: true
