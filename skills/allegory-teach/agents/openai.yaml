interface:
  display_name: "Allegory Teach"
  short_description: "Teach hard concepts with precise bridges or causal fables."
  default_prompt: "Use $allegory-teach to explain one difficult concept bridge-first, switching to an adaptive-length concealed-name fable that runs a checkable in-story case only if I explicitly request it."

policy:
  allow_implicit_invocation: true
